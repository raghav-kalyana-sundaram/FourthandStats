# Metric Definitions

> Placeholder — populated in Part D (C.7 / D metric implementation).

See `config/metric_definitions.yaml` for the full machine-readable glossary.

Each metric entry will document: name, description, formula, source columns, higher-is-better flag, units, and sample-size caveats.
