# Rating Methodology

> Placeholder — populated in Part D (D.4).

Ratings are weighted percentile composites. Each rating combines multiple per-season percentile ranks into a 0–100 composite score.

Weights are defined in `config/rating_weights.yaml` and are fully configurable without code changes.

Each rating exposes component breakdowns so every score is traceable back to specific metric values.
